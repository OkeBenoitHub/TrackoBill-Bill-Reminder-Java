<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.trackobill.www">

    <!-- App Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_label"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <!-- Sample AdMob App ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id"/>

        <receiver android:name=".RecurringBillAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recurring_bill_app_widget_info" />
        </receiver>

        <!-- This is required for immediate syncs -->
        <service
            android:name=".services.RecurringBillReminderIntentService"
            android:exported="false" />

        <!-- Bill On Hold Activity -->
        <activity
            android:name=".activities.BillsOnHoldActivity"
            android:label="@string/bills_on_hold_activity_name"
            android:parentActivityName=".activities.ViewBillsActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.ViewBillsActivity" />
        </activity> <!--  -->
        <!-- Settings Activity -->
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/settings_activity_name"
            android:parentActivityName=".activities.ViewBillsActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.ViewBillsActivity" />
        </activity> <!--  -->
        <!-- Paid Bills Activity -->
        <activity
            android:name=".activities.PaidBillsActivity"
            android:label="@string/paid_bills_activity_name"
            android:parentActivityName=".activities.ViewBillsActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.ViewBillsActivity" />
        </activity> <!--  -->
        <!-- Add New Bill Activity -->
        <activity
            android:name=".activities.RecurringBillActivity"
            android:label="@string/bill_text"
            android:parentActivityName=".activities.ViewBillsActivity">

            <intent-filter>
                <action android:name="android.intent.action.INSERT" />
                <data android:mimeType="vnd.android.cursor.dir/event" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.ViewBillsActivity" />
        </activity> <!--  -->
        <!-- View Bills Activity -->
        <activity
            android:name=".activities.ViewBillsActivity"
            android:label="@string/view_bills_activity_name"
            android:parentActivityName=".activities.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity> <!--  -->
        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>